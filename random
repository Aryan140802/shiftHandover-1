import React, { useEffect, useState, useCallback, useRef } from 'react';
import { Menu as MenuIcon, ChevronLeft, ChevronRight, Star } from 'lucide-react';
import { cardLinkEISInfra, cardLinkSupport } from './CardLinks';
import  { apiFetchFavorites, apiUpdateFavorites } from "../api/PostApi";

import '../styles/Menu.css';

const Sidebar = ({ darkMode, isSidebarOpen, setIsSidebarOpen }) => {
  const [searchTerm, setSearchTerm] = useState('');
  const [favorites, setFavorites] = useState([]);
  const [userId, setUserId] = useState(null);
  const [password, setPassword] = useState(null);
  const [userLevel, setUserLevel] = useState(null);
  const [cardLinks, setCardLinks] = useState([]);
  
  // Use ref to keep track of latest favorites
  const favoritesRef = useRef(favorites);
  
  // Update ref whenever favorites changes
  useEffect(() => {
    favoritesRef.current = favorites;
  }, [favorites]);

  const toggleSidebar = () => setIsSidebarOpen(prev => !prev);

  // Get userId, password, and userLevel from localStorage on mount
  useEffect(() => {
    const uid = localStorage.getItem('uidd');
    const pwd = localStorage.getItem('password');
    const level = localStorage.getItem('userlevel');

    if (uid && pwd) {
      setUserId(uid);
      setPassword(pwd);
    } else {
      console.warn('No user credentials found in localStorage');
    }

    if (level) {
      setUserLevel(level);
    } else {
      console.warn('No user level found in localStorage');
    }
  }, []);

  // Set cardLinks based on userLevel
  useEffect(() => {
    if (!userLevel) return;

    const normalizedLevel = userLevel.toUpperCase();

    switch (normalizedLevel) {
      case 'ADMIN':
      case 'L2':
        setCardLinks(cardLinkEISInfra);
        break;
      case 'SUPPORT':
        setCardLinks(cardLinkSupport);
        break;
      case 'BANK':
        // Combine both arrays for Bank users
        setCardLinks([...cardLinkEISInfra, ...cardLinkSupport]);
        break;
      default:
        console.warn('Unknown user level:', userLevel);
        setCardLinks([]);
    }
  }, [userLevel]);

  // Fetch favorites from backend using the API function
  useEffect(() => {
    if (!userId || !password) return;

    const loadFavorites = async () => {
      try {
        const favArray = await apiFetchFavorites();
        console.log('Loaded favorites:', favArray);
        setFavorites(favArray);
      } catch (err) {
        console.error('Error loading favorites:', err);
      }
    };

    loadFavorites();
  }, [userId, password]);

  // Toggle favorite - FIXED VERSION using functional state update
  const toggleFavorite = async (name) => {
    console.log('Toggling favorite:', name);
    console.log('Current favorites before toggle:', favorites);

    // Use functional state update to get the latest state
    setFavorites((prevFavorites) => {
      const updatedFavorites = prevFavorites.includes(name)
        ? prevFavorites.filter((fav) => fav !== name)
        : [...prevFavorites, name];

      console.log('Updated favorites:', updatedFavorites);

      // Update backend with the new favorites array
      apiUpdateFavorites(updatedFavorites)
        .then(() => {
          console.log('Successfully updated favorites on backend');
        })
        .catch((error) => {
          console.error('Failed to update favorites on backend:', error);
          // Revert to previous state on error
          setFavorites(prevFavorites);
        });

      return updatedFavorites;
    });
  };

  // Filter and sort links
  const filteredLinks = cardLinks
    .filter((link) => link.name.toLowerCase().includes(searchTerm.toLowerCase()))
    .sort((a, b) => {
      const aFav = favorites.includes(a.name);
      const bFav = favorites.includes(b.name);
      return aFav === bFav ? 0 : aFav ? -1 : 1;
    });

  return (
    <>
      {!isSidebarOpen && (
        <button className="sidebar-toggle" onClick={toggleSidebar}>
          <MenuIcon size={24} />
        </button>
      )}

      <div className={`sidebar ${isSidebarOpen ? 'open' : 'collapsed'}`}>
        <div className="sidebar-header">
          {isSidebarOpen && <h2 className="sidebar-title">Quick Access</h2>}
          <button
            className="collapse-btn"
            onClick={toggleSidebar}
            aria-label={isSidebarOpen ? 'Collapse sidebar' : 'Expand sidebar'}
          >
            {isSidebarOpen ? <ChevronLeft size={20} /> : <ChevronRight size={20} />}
          </button>
        </div>

        {isSidebarOpen && (
          <div className="sidebar-search">
            <input
              type="text"
              placeholder="Search links..."
              value={searchTerm}
              onChange={(e) => setSearchTerm(e.target.value)}
              className="search-input"
            />
          </div>
        )}

        <div className="sidebar-content">
          <div className="sidebar-section">
            {filteredLinks.length > 0 ? (
              filteredLinks.map((link, index) => {
                const isFavorited = favorites.includes(link.name);
                return (
                  <div key={index} className="link-card">
                    <span
                      className="star-icon"
                      onClick={() => toggleFavorite(link.name)}
                      title={isFavorited ? 'Unstar' : 'Star'}
                      style={{ cursor: 'pointer' }}
                    >
                      {isFavorited ? (
                        <Star fill="#facc15" color="#facc15" size={16} />
                      ) : (
                        <Star color="#fff" size={16} />
                      )}
                    </span>
                    <a
                      href={link.url}
                      target="_blank"
                      rel="noopener noreferrer"
                      className="link-anchor"
                    >
                      {isSidebarOpen ? (
                        <span>{link.name}</span>
                      ) : (
                        <img
                          src={link.icon}
                          alt={link.name}
                          title={link.name}
                          className="link-icon"
                        />
                      )}
                    </a>
                  </div>
                );
              })
            ) : (
              <p className="no-links-message">No links available for your user level</p>
            )}
          </div>
        </div>
      </div>
    </>
  );
};

export default Sidebar;
